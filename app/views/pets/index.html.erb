<div class="container mt-5 w-50">
  <h1 class="text-center mb-4">Pets 🐕🐱🐤🐇 </h1>
  <%= link_to 'Add a pet', pets_new_path, class: 'btn btn-dark my-3' %>

  <ul class="list-group">
    <% @pets.each do |pet| %>
      <li class="list-group-item"><%= link_to pet.name, pet_path(pet), class: 'text-decoration-none' %></li>
    <% end %>
  </ul>
</div>
